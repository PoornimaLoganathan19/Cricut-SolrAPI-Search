import { Address } from "./address";

export class User {
  firstName: string;
  lastName: string;
  userId: string;
  title: string;
  phoneNumber: string;
  address: Address[];
  dealershipName: string;
  website: string;
}
